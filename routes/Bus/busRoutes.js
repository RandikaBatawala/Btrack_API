const { route } = require("./busMain");

module.exports = function(router){
    var bodyParser = require("body-parser");
	var jsonParser = bodyParser.json();
    const busController = require("../../controllers/busController");
   

};